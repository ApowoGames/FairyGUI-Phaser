namespace fgui {
    export class GGroup extends GObject {
        protected isEmpty: boolean;
        constructor(scene: Phaser.Scene) {
            super(scene);
        }
        // todo createDisplayObject

    }
}